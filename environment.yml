name: cmpt-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - fastapi>=0.100.0
      - uvicorn>=0.23.0
      - sqlalchemy>=2.0.0
      - psycopg2-binary>=2.9.0
      - pydantic>=2.0.0
      - python-dotenv>=1.0.0
      - crewai>=0.1.0
      - firecrawl-py>=0.0.1  # Firecrawl Python SDK
      - pyyaml>=6.0
